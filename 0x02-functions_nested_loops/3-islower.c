#include "main.h"
/**
 *main - entry point
 *Description: show 1 if the input is a otherwise show 0
 *Return: return 1 for lowercase return 0 otherwise
 */
int _islower(int c)
{
	if (c > = 97; && c <= 122;)
	{
	return (1);
	}
		else
		{
		return (0);
		}
	_putchar('\n');
}
